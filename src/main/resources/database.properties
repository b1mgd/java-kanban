# Database connection settings
db.url=jdbc:postgresql://localhost:5432/kanban
db.user=postgres
db.password=postgres

# Connection pool settings
db.pool.size=10
db.pool.timeout=30000
db.pool.maxLifetime=1800000 